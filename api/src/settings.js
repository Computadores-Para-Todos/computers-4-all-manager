/**
 * Constantes e configurações
 */
// TODO: variáveis de ambiente aqui (valores default etc)

// Roles do sistema
const ROLES = {
  USER: 'user',
  ADMIN: 'admin'
};

module.exports = {
  ROLES
};
